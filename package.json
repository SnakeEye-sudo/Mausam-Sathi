{
  "name": "mausam-sathi",
  "private": true,
  "version": "1.0.0",
  "description": "Weather planner with city search, current conditions, five-day outlook, and browser geolocation powered by Open-Meteo.",
  "scripts": {
    "build": "esbuild src/app.ts --bundle --format=iife --target=es2020 --outfile=app.js"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
